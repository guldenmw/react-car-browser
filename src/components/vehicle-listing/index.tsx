import React, { FC } from 'react';

interface IProps {
  [x: string]: any;
}

const VehicleListing: FC<IProps> = (props) => {
  const {  } = props;
  return (
    <div>

    </div>
  );
};

VehicleListing.defaultProps = {};

export default VehicleListing;
